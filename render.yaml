services:
  - type: web
    name: sentra-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      # Add these in Render Dashboard -> Environment (manual)
      # - DISCORD_CLIENT_ID
      # - DISCORD_CLIENT_SECRET
      # - DISCORD_REDIRECT_URI